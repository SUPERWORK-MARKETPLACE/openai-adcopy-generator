# CLAUDE.md — ChatGPT 광고 대화 맥락·카피 생성 에이전트

> 매 세션 시작 시 로드되는 **운영 매뉴얼**이다. 코드에서 추론 가능한 내용은 걷어내고
> **결정을 바꾸는 정보만** 남긴다. 항상 500줄 이하로 유지한다.

---

## 0. 한 줄 정의

광고주가 제공한 **상품·타깃·혜택·이미지·정책** 정보를 기반으로 다양한 **대화 맥락**을 확장하고,
**광고그룹 · Context Hints · 제목 · 카피**를 대량으로 **생성 → 검수 → 출력**하는 에이전트.
향후 광고 성과 데이터를 반영해 더 나은 신규 카피를 지속 생성·제안한다.

> **IMPORTANT — 핵심 관점:** 이것은 "엑셀 변환기"가 **아니다**. 문맥별 광고 문구를
> **확장·검수·출력**하는 에이전트다. 입력 셀을 출력 셀로 매핑하는 문제로 축소해서 접근하지 마라.

---

## 1. 코딩 원칙 — Andrej Karpathy's 4 Rules (반드시 준수)

LLM은 예측 가능한 실패 패턴을 가진다(조용한 가정, 과설계, 범위 이탈, 목표 불명확). 아래 4개
규칙으로 이를 방어한다. 이 프로젝트의 코드/문서/카피 생성 로직 모두에 적용한다.

### 1) Think Before Coding — *"Don't assume. Don't hide confusion. Surface tradeoffs."*
- 구현 전에 **가정을 명시**하고, 불확실하면 **추측하지 말고 물어라**.
- 해석이 여러 개면 **조용히 하나를 고르지 말고** 모두 제시한다.
- 더 단순한 대안이 있으면 제안하고, 헷갈리는 지점은 이름 붙여 드러낸다.
- *이 프로젝트 적용:* 스펙(제약·필드·정책)이 애매하면 코드를 짜기 전에 확인한다. 도메인 규칙을
  임의로 추정하지 마라.

### 2) Simplicity First — *"Minimum code that solves the problem. Nothing speculative."*
- 요청된 것만 구현한다. 추가 기능·불필요한 추상화·요청되지 않은 예외 처리를 넣지 마라.
- 200줄로 짠 것이 50줄로 될 것 같으면 다시 쓴다. "시니어 엔지니어가 과설계라고 할까?"를 자문한다.
- *이 프로젝트 적용:* 2차(후속) 기능을 1차 코드에 미리 끼워넣지 마라. §7 범위 참조.

### 3) Surgical Changes — *"Touch only what you must. Clean up only your own mess."*
- 기존 코드 수정 시 **스타일을 보존**하고 무관한 부분을 리팩터링하지 마라.
- 내 수정으로 쓸모없어진 import/함수만 제거한다. 기존 dead code는 **삭제하지 말고 표시**만 한다.
- 바뀐 모든 줄이 요청과 직접 연결되어야 한다.

### 4) Goal-Driven Execution — *"Define success criteria. Loop until verified."*
- 모호한 요청을 **검증 가능한 성공 기준**으로 바꾼 뒤, 각 단계에 확인 지점을 둔다.
- 예: "검수 붙여줘" → "잘못된 입력에 대한 테스트를 쓰고 통과시켜라".
- *이 프로젝트 적용:* 검수·형식 규칙(§6)은 성공 기준이 명확하다. 테스트/검증으로 자동 반복하라.

> 출처: Andrej Karpathy CLAUDE.md skills — https://github.com/multica-ai/andrej-karpathy-skills

---

## 2. 개발 명령어 (Build · Test · Run)

> **가장 가치가 높은 섹션이다. 스택을 확정하는 즉시 여기부터 채운다.** (Rule 1: 스택은 임의로
> 정하지 말고 확인할 것.)

**환경 (확인됨):**
- OS: Windows 11 / 기본 셸 **PowerShell** (Bash도 사용 가능, 각자 문법 준수)
- Node.js 설치됨 · Python 설치됨(pyenv, 3.12) · `pdftotext`(Git) 사용 가능

**명령어 (스캐폴딩 후 확정):**
```
build:  cd tool; powershell -ExecutionPolicy Bypass -File .\build.ps1   # bin/ 3종 크로스컴파일
test:   cd tool; go test ./...
lint:   cd tool; go vet ./...
run:    cd tool; go run ./cmd/adcopy <command>                           # 개발용 직접 실행
```

---

## 3. 현재 상태 & 원본 자료

- **상태:** v0.2 구현 완료(Claude Code 플러그인 — 대량 생성 모드 포함). 구조: `tool/`(Go CLI adcopy) ·
  `bin/`(3종 바이너리) · `skills/`·`commands/`(플러그인) · `.claude-plugin/`(매니페스트). 설계·계획: `docs/superpowers/`.
- **원본 문서는 `etc/`에 로컬 보관(저장소 미포함, 2026-07-03 이동)** — 개발요청서 PDF(v2)와
  통합 워크북 템플릿들. DRM(매직바이트 `SCDSA...`)으로 암호화되어 코드에서 직접 파싱 불가하고
  PDF는 CJK 폰트 문제로 텍스트 추출이 깨지므로(이미지 렌더링으로 검토 완료), 핵심 내용은 모두
  이 문서에 반영되어 있다. 실제 광고주 업로드 파일은 표준 OOXML(.xlsx)로 가정한다.
  - **개발요청서 v2 핵심 변경:** 확장 축의 `의도`가 `구매 여정·의도`로 확대되고, **구매 여정 퍼널
    6단계**(§5)가 광고그룹·Context Hints·제목·카피 생성의 분류 축으로 신설. 광고그룹명
    **특수문자 가능** 확정, Context Hints 생성 유형 분류 추가.
- `data/오픈AI 광고 세팅용 스프레드 시트 - 캐츠잉글리시.xlsx` — **복호화된 실물 업로드 시트**
  (표준 OOXML, 파싱 가능). 공식 3시트 컬럼 구조의 기준(golden reference). max_bid 전 행 빈칸.

---

## 4. 도메인 모델

### 역할 & Human-in-the-loop (절대 생략 금지)
`광고주/대행사` → (정보 제공) → `AI 에이전트` → (생성) → `운영자` → (검수·수정) → `광고주` → (확인) → 업로드 → (시스템에서 max_bid 수동 설정)
- **AI는 `adgroups`·`ads`를 생성만 한다. 자동 업로드하지 않는다.** 운영자 검수와 광고주 확인을
  거친 뒤에만 최종 파일이 만들어진다.
- **max_bid는 최종 파일에서 항상 빈칸**(2026-07-02 확정: 값을 넣으면 업로드 오류 발생).
  업로드 후 광고 시스템에서 운영자가 수동 입력한다.

### 통합 워크북 시트 구조
| 구분 | Sheet | 작성 주체 | 역할 | 공식 업로드 |
|---|---|---|---|---|
| 입력 | `campaigns` | 광고주/대행사 | 캠페인명·예산·기간·목표·국가 | **포함** |
| 입력 | 상품·브리프 | 광고주 | 상품·혜택·타깃·니즈·조건·대표 랜딩 URL | 제외 |
| 입력 | 이미지소재 | 광고주 | `image_link`·이미지 메시지·규격·소재별 랜딩 URL | 제외 |
| 입력 | 고객질문·검색데이터 | 광고주 | FAQ·상담·검색 자료 링크 | 제외 |
| 입력 | 상품별 주력 키워드 | 광고주 | SA·사이트 검색어·세일즈 키워드 | 제외 |
| 입력 | 정책·참고자료 | 광고주 | 톤앤매너·금지어·법무/심의 기준 | 제외 |
| 출력 | `adgroups` / `ads` | **AI 생성 + 운영 검토** | Context Hints·제목·카피 자동 생성 | **포함** |

### 생성 결과 → OpenAI 공식 템플릿 필드 매핑
| 생성 결과 | 템플릿 필드 | 처리 |
|---|---|---|
| 캠페인명 | `campaigns.campaign_name` | 광고주·대행사 입력 |
| 광고그룹명 | `adgroups.adgroup_name` | AI 생성 |
| Context Hints | `adgroups.keywords` | AI 생성 → **JSON 배열** |
| 광고명 | `ads.ad_name` | AI 생성 — **실물 시트에 컬럼 없음** → 내부 관리 필드(추적·중복 방지용), 업로드 파일에서 제외 |
| 제목 | `ads.title` | AI 생성 |
| 카피 | `ads.copy` | AI 생성 |
| 랜딩/이미지 URL | `ads.link`, `ads.image_link` | 입력값 자동 연결 (하나라도 없으면 알람) |
| max_bid | `adgroups.max_bid` | **항상 빈칸** — 업로드 후 시스템에서 수동 입력(값 넣으면 오류) |

- **최종 업로드 파일은 공식 3개 시트(`campaigns` / `adgroups` / `ads`)만** 추출한다.
- **공식 시트 확정 컬럼**(실물 캐츠잉글리시 시트 기준, §3):
  - `campaigns`: campaign_name · budget_max · budget_type · launch_date · end_date · objective · target_countries(JSON 배열 문자열, 예 `["KR"]`)
  - `adgroups`: campaign_name · adgroup_name · max_bid(빈칸) · keywords(JSON 배열 문자열)
  - `ads`: adgroup_name · title · copy · link · image_link
- **근거 추적 필드**(검수·설명용, 별도 관리): `source_type`, `source_url`, `source_excerpt`,
  `generation_basis`, `validation_status`, `review_comment`, `confidence_score`.

---

## 5. 생성 파이프라인 (순서 고정)

```
입력 분석·구조화
  → 대화 맥락 확장  (SKU × Persona × 고객 문제 × 상황 × 구매 여정·의도 × 메시지 방향)
  → 광고그룹 생성   (유사한 대화 주제·메시지 방향 묶음 = 운영 단위)
  → Context Hints  (광고그룹당 5~10개 이상, JSON 배열)
  → 제목·카피 세트 대량 생성  (Context Hints를 충분히 커버, 다양한 메시지 각도)
  → 이미지·랜딩 URL 연결
  → 자동 검수·재생성
  → 근거 추적 기록
  → 공식 시트 출력
```

- **상품 정보를 바로 카피로 바꾸지 마라.** 대화 맥락을 먼저 구조화한다.
- **모든 조합을 기계적으로 만들지 마라.** 상품이 **자연스러운 해결책이 되는 맥락만** 선별한다.
- **출처 우선순위(충돌 시 상위 우선):** ①법무·심의·정책 → ②광고주 브리프 → ③대표 랜딩 URL →
  ④소재별 랜딩 URL·이미지 내 문구 → ⑤고객 질문·검색 데이터 → ⑥AI 추론.

### 구매 여정 퍼널 (v2 신설 — 생성·분류의 핵심 축)

`①문제 정의 → ②제품 발견 → ③비교 검토 → ④단일 제품 평가 → ⑤구매·신청 → ⑥사용·문제 해결`

- 퍼널 단계는 광고그룹·Context Hints·제목·카피 생성의 **핵심 분류 축**. **동일 상품도 단계별로
  질문 맥락과 메시지 초점을 달리해** 생성한다(예: 비교 검토 단계 → "생활비 카드 비교" 힌트,
  비교 기준·혜택 차이 중심 카피).
- Context Hints는 각 단계에서 사용자가 **실제 입력할 법한 질문·고민·상황**으로, 제목·카피는
  해당 단계의 **정보 요구 수준**에 맞춰 작성한다.
- 전 단계를 기계적으로 동일 수량 생성하지 말고 **캠페인 목적·상품 특성에 따라 비중을 조정**한다.
  사용·문제 해결 단계는 선택 적용.
- 퍼널 단계는 **구매 가능성 단정이 아니라 질문 목적·대화 맥락의 분류 기준**이다(§7 민감 주제
  규칙과 함께 적용). 비교·평가 단계에서 경쟁사 비방·확인되지 않은 우월성/비교 표현 금지.
- **운영자 설정:** 포함·제외할 퍼널 단계, 퍼널 단계별·세부 의도별 생성 비중, 상품·광고그룹별
  개별 적용. AI 추천 비중을 기본값으로 제공하고 운영자가 조정한다.

### 광고그룹·Context Hints 생성 기준 (v2 구체화)

- **광고그룹 판단 흐름:** 입력 정보(SKU·Persona·상황) → 구매 여정(단계·세부 의도) → 운영 조건
  (랜딩·정책·입찰) → 그룹 결정(묶기·분리·캠페인 분리).
  - 구매 여정 단계·세부 의도가 유사하면 묶고, 여정 단계·의도가 달라 메시지 방향·Context Hints·
    랜딩 연결을 동일하게 운영하기 어려우면 나눈다. 단, **여정 단계가 다르더라도 메시지 방향·랜딩·
    정책 조건을 동일하게 운영할 수 있으면 같은 그룹으로 묶을 수 있다.**
  - 캠페인 목적·국가/지역·예산/기간이 다르면 광고그룹이 아니라 **캠페인 분리** 기준으로 관리.
- **Context Hints 생성 유형:** 구매 여정 유형(문제 정의형/제품 발견형/비교 검토형/단일 제품
  평가형/구매·신청형/사용·문제 해결형) × 표현 형식(주제형/질문형/문제형/상황형)을 혼합해 생성.

---

## 6. 하드 제약 (형식 검증 규칙)

| 대상 | 규칙 |
|---|---|
| `title` (제목) | 권장 16~18자 / **최대 24자**. 상품명만 반복 금지. |
| `copy` (카피) | 권장 32~36자 / **최대 48자**. 제목·동일 혜택 반복 금지. 제공된 사실 내에서만. |
| `keywords` (Context Hints) | 광고그룹당 **5~10개 이상**, JSON 배열, 한국어/영어. **수량 채우려 관련성 낮은 힌트 생성 금지.** |
| 이미지 | 정사각형 PNG/JPG, **1:1**, **640×640 ~ 1200×1200px**, 로고 메인 금지(로고 메인 여부는 AI 판별 후 운영자 확인). |
| `adgroup_name` | 형식 `상품·SKU_고객유형_상황/문제_의도/메시지`, 3~1,000자, 공백만 구성 금지, **특수문자 가능**(플랫폼 허용 문자 기준 자동 정규화, 형식 검수 대상), 중복 시 순번/고유 코드. |
| `ad_name` | 형식 `campaign 또는 SKU 코드 + adgroup 코드 + creative 순번`, 중복 금지. |
| URL | 접근 가능 여부·HTTPS·리디렉션 도메인·프로모션 종료·이미지 만료 검증. **접근 실패 시 생성 전에 오류 표시.** |
| 수치·가격·기간 | 랜딩페이지와 **정확히 일치**해야 한다. |

**업로드 전 검증(필수):** `campaign`/`adgroup` 참조 무결성 · JSON 유효성 · 중복 `ad_name` ·
URL 형식 · 필수값 · 숫자 타입.

---

## 7. 반드시 지켜야 할 규칙 (YOU MUST / NEVER)

- **사실성:** 제공된 사실 안에서만 생성한다. **미제공 혜택·조건을 지어내지 마라(환각 금지).**
- **근거 추적:** 모든 생성물에 출처(`source_*`)와 `generation_basis`(SKU·Persona·고객 문제·
  상황·**구매 여정 단계·세부 의도**·메시지 방향)를 남긴다. 근거 없으면
  `validation_status = 확인 필요`.
- **정보 충돌:** 브리프·랜딩·이미지·소재별 URL 간 정보가 다르면 **자동 생성하지 말고 '확인 필요' 표시.**
- **자동 검수의 지위:** 자동 검수는 **오류 후보 선별일 뿐**이다. 최종 판단·승인은 **운영자·광고주**가 한다.
- **경쟁사명:** 기본 제외. 광고주가 입력해도 자동 사용 금지, **승인된 이름만** 허용 목록에서 호출.
  사용 승인·권리/비교광고 검토 여부는 **별도 필드로 관리**. 비방·우월성 단정·확인되지 않은 비교
  표현은 **생성 금지**. 경쟁사명만 포함되거나 자사 상품과 해결 연관성 없는 Context Hints는 제거.
- **개인정보:** 상담·챗봇 데이터의 이름/전화번호/이메일/카드번호 등은 **비식별화**한다.
- **민감 주제:** 금융·건강 등 개인 상태를 추정하는 표현 금지.
- **프로모션 종료:** 종료일이 지난 소재는 생성·출력 대상에서 자동 제외.
- **질문 출처 구분:** 실제 고객 데이터에서 추출한 질문과 AI가 추론한 예상 질문을 **결과에서 구분 표시**.
- **수량보다 품질:** 목표 수량을 채우려 동일 의미 문구를 반복 생성하지 마라. 확장 축이 부족하면
  **요청 수량보다 적게 출력**할 수 있다(제외 사유 표시).
- **검수 상태값:** `무수정 승인` / `수정 후 승인` / `사용 불가` / `부분 재생성` / `광고주 확인 필요`.

---

## 8. 범위: 1차 vs 2차 (경계를 넘지 마라)

- **1차 (지금 만든다):** 워크북 읽기 → 맥락 확장(Persona·문제·상황·**구매 여정 단계·세부 의도**·
  메시지 방향 추출) → 광고그룹·Context Hints·제목·카피 생성 → 글자수·금지어·사실성·중복·형식·URL
  검수 → 운영자 조건 설정(수량·우선순위·퍼널 비중·검수 기준) 및 결과 검토 화면 → 승인/재생성 →
  근거·제외 사유 표시 → 공식 `campaigns`/`adgroups`/`ads` 파일 출력.
- **2차 (후속, 지금 구현하지 마라):** 성과 API 자동 연동(Advertiser API Insights; 전환·CPA·ROAS
  분석은 별도 전환 측정 설정 필요), CTR·CVR·CPA·ROAS 기반 최적화, 우수·저성과 분석 기반 카피
  재생성, 실시간 정책 자동 업데이트, 법무 검수 자동화, 계정별 대시보드, 광고주별 정책 템플릿화,
  우수 카피 자동 추천·교체.

---

## 9. 코드 스타일 & 컨벤션

- **스펙 필드명을 그대로 사용한다:** `campaign_name`, `adgroup_name`, `keywords`, `ad_name`,
  `title`, `copy`, `link`, `image_link`, `max_bid`, `source_type` 등. **임의로 개명하지 마라.**
- Context Hints는 항상 `keywords` 필드에 **JSON 배열**로 넣는다.
- 사용자·문서 커뮤니케이션은 **한국어**, 코드 식별자·주석 키워드는 영어.
- 광고그룹명·광고명에 **특수문자 사용 가능(v2 확정)**. 단, 플랫폼 허용 문자 기준으로 자동
  정규화하고 형식 검수 대상에 포함한다.
- 임시/실험 파일은 저장소가 아니라 세션 스크래치패드에 둔다.

---

## 10. 용어집

- **SKU** — 광고할 상품·서비스 단위. **Persona** — 상품 니즈가 있는 고객 유형.
- **구매 여정(퍼널)** — `문제 정의 → 제품 발견 → 비교 검토 → 단일 제품 평가 → 구매·신청 →
  사용·문제 해결` 6단계. 구매 가능성 단정이 아니라 **질문 목적·대화 맥락의 분류 기준**(§5).
- **Context Hints** — 광고가 관련될 수 있는 대화 주제·질문·문제·상황·의도를 설명하는 신호.
  정확 일치 키워드가 아니라 **문맥 매칭용 입력값**이며, 업로드 시 `keywords` 필드에 매핑된다.
- **대표 랜딩 URL** — 상품 이해·기본 정보 분석의 기준. **소재별 랜딩 URL** — `ads.link`에 우선
  적용(미입력 시 대표 랜딩 URL).
- **운영자** — 생성 조건 설정·검수·다운로드 담당. **광고주/대행사** — 정보 제공·최종 확인·`max_bid` 확정.
