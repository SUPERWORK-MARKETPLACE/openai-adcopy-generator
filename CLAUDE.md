# CLAUDE.md — ChatGPT 광고 대화 맥락·카피 생성 에이전트

> 매 세션 시작 시 로드되는 **운영 매뉴얼**이다. 코드에서 추론 가능한 내용은 걷어내고
> **결정을 바꾸는 정보만** 남긴다. 항상 500줄 이하로 유지한다.

---

## 0. 한 줄 정의

광고주가 제공한 **상품·타깃·혜택·이미지·정책** 정보를 기반으로 다양한 **대화 맥락**을 확장하고,
**광고그룹 · Context Hints · 제목 · 카피**를 대량으로 **생성 → 검수 → 출력**하는 에이전트.
향후 광고 성과 데이터를 반영해 더 나은 신규 카피를 지속 생성·제안한다.

> **IMPORTANT — 핵심 관점:** 이것은 "엑셀 변환기"가 **아니다**. 문맥별 광고 문구를
> **확장·검수·출력**하는 에이전트다. 입력 셀을 출력 셀로 매핑하는 문제로 축소해서 접근하지 마라.

---

## 1. 코딩 원칙 — Andrej Karpathy's 4 Rules (반드시 준수)

LLM은 예측 가능한 실패 패턴을 가진다(조용한 가정, 과설계, 범위 이탈, 목표 불명확). 아래 4개
규칙으로 이를 방어한다. 이 프로젝트의 코드/문서/카피 생성 로직 모두에 적용한다.

### 1) Think Before Coding — *"Don't assume. Don't hide confusion. Surface tradeoffs."*
- 구현 전에 **가정을 명시**하고, 불확실하면 **추측하지 말고 물어라**.
- 해석이 여러 개면 **조용히 하나를 고르지 말고** 모두 제시한다.
- 더 단순한 대안이 있으면 제안하고, 헷갈리는 지점은 이름 붙여 드러낸다.
- *이 프로젝트 적용:* 스펙(제약·필드·정책)이 애매하면 코드를 짜기 전에 확인한다. 도메인 규칙을
  임의로 추정하지 마라.

### 2) Simplicity First — *"Minimum code that solves the problem. Nothing speculative."*
- 요청된 것만 구현한다. 추가 기능·불필요한 추상화·요청되지 않은 예외 처리를 넣지 마라.
- 200줄로 짠 것이 50줄로 될 것 같으면 다시 쓴다. "시니어 엔지니어가 과설계라고 할까?"를 자문한다.
- *이 프로젝트 적용:* 2차(후속) 기능을 1차 코드에 미리 끼워넣지 마라. §7 범위 참조.

### 3) Surgical Changes — *"Touch only what you must. Clean up only your own mess."*
- 기존 코드 수정 시 **스타일을 보존**하고 무관한 부분을 리팩터링하지 마라.
- 내 수정으로 쓸모없어진 import/함수만 제거한다. 기존 dead code는 **삭제하지 말고 표시**만 한다.
- 바뀐 모든 줄이 요청과 직접 연결되어야 한다.

### 4) Goal-Driven Execution — *"Define success criteria. Loop until verified."*
- 모호한 요청을 **검증 가능한 성공 기준**으로 바꾼 뒤, 각 단계에 확인 지점을 둔다.
- 예: "검수 붙여줘" → "잘못된 입력에 대한 테스트를 쓰고 통과시켜라".
- *이 프로젝트 적용:* 검수·형식 규칙(§6)은 성공 기준이 명확하다. 테스트/검증으로 자동 반복하라.

> 출처: Andrej Karpathy CLAUDE.md skills — https://github.com/multica-ai/andrej-karpathy-skills

---

## 2. 개발 명령어 (Build · Test · Run)

> **가장 가치가 높은 섹션이다. 스택을 확정하는 즉시 여기부터 채운다.** (Rule 1: 스택은 임의로
> 정하지 말고 확인할 것.)

**환경 (확인됨):**
- OS: Windows 11 / 기본 셸 **PowerShell** (Bash도 사용 가능, 각자 문법 준수)
- Node.js 설치됨 · Python 설치됨(pyenv, 3.12) · `pdftotext`(Git) 사용 가능

**명령어 (스캐폴딩 후 확정):**
```
build:  cd tool; powershell -ExecutionPolicy Bypass -File .\build.ps1   # bin/ 3종 크로스컴파일
test:   cd tool; go test ./...
lint:   cd tool; go vet ./...
run:    cd tool; go run ./cmd/adcopy <command>                           # 개발용 직접 실행
```

---

## 3. 현재 상태 & 원본 자료

- **상태:** v0.3 구현 완료(Claude Code 플러그인 — 대량 생성 모드·워크북 v2 대응). 구조: `tool/`(Go CLI adcopy) ·
  `bin/`(3종 바이너리) · `skills/`·`commands/`(플러그인) · `.claude-plugin/`(매니페스트). 설계·계획 문서는 `docs/superpowers/`에 로컬 보관(저장소 미포함).
- **원본 문서는 `etc/`에 로컬 보관(저장소 미포함, 2026-07-03 이동)** — 개발요청서 PDF(v2)와
  통합 워크북 템플릿들. DRM(매직바이트 `SCDSA...`)으로 암호화되어 코드에서 직접 파싱 불가하고
  PDF는 CJK 폰트 문제로 텍스트 추출이 깨지므로(이미지 렌더링으로 검토 완료), 핵심 내용은 모두
  이 문서에 반영되어 있다. 실제 광고주 업로드 파일은 표준 OOXML(.xlsx)로 가정한다.
  - **개발요청서 v2 핵심 변경:** 확장 축의 `의도`가 `구매 여정·의도`로 확대되고, **구매 여정 퍼널
    6단계**(§5)가 광고그룹·Context Hints·제목·카피 생성의 분류 축으로 신설. 광고그룹명
    **특수문자 가능** 확정, Context Hints 생성 유형 분류 추가.
- `data/오픈AI 광고 세팅용 스프레드 시트 - 캐츠잉글리시.xlsx` — **복호화된 실물 업로드 시트**
  (표준 OOXML, 파싱 가능). 공식 3시트 컬럼 구조의 기준(golden reference). max_bid 전 행 빈칸.
- `workbooks/` — 광고주 제공용 통합워크북 템플릿(캐츠잉글리시 v1·v2, 표준 OOXML). **v2가 입력
  시트 구조의 기준.** v2 변경(2026-07-13): 상품·브리프의 "필수 포함 문구" → **"카피 핵심 반영
  요소"** — verbatim 필수가 아니라 `/` 구분 **후보군**(§4·§6). `required_phrases` 필드·
  `required_phrase_missing` 검사는 제거됨.
- **생성 로직 수정 요청사항 PDF(2026-07-13, 저장소 루트, 4p)** — R1~R6: 퍼널 단계명 **6개 고정
  토큰**(§5), adgroup_name `상품/SKU_타깃_세부의도_구매여정`(§6), Context Hints 질문형·상황형
  우선·그룹 간 중복 통제(§6), 카피 종결형 분산·CTA 30% 제한(§6), 플래그 행 트리거(§7 —
  기본값 자동 기입은 0715로 폐지).
- **2차 수정 요청사항(2026-07-14, `workbooks/에이전트 생성 로직 수정 요청사항.txt`)** — S1
  validation_status 플래그 어휘 8종(§7) · S2 검색어형 40/60±10·구체화(요소 2개 이상, §6) · S4 copy 완결 문장
  (`copy_sentence_form`, §6) · S5 title+copy 연결성(§6).
- **내용 검수 후속(2026-07-14)** — ai_inferred 사실 주장 금지·`source_excerpt` 필수(§7),
  `title_copy_overlap`·`copy_speech_level`(§6).
- **3차 수정 요청사항(2026-07-15, `workbooks/에이전트 생성 로직 수정 요청사항_0715.txt`)** —
  ①검수상태=광고주 검수 결과 입력란: **전 행 빈칸 디폴트·자동 기입 폐지**, 상태값 4종으로 축소
  (`광고주 확인 필요`는 validation_status 전용 플래그, §7) · ②copy 자연성 강화(설명문투·어색한
  조사/어순·딱딱한 서술어 회피)·title-copy 연결성(고민 반복·재질문 금지 — 기계 검사
  `title_copy_question_repeat` 신설, §6).
- **4차 산출물 검토 후속(2026-07-15)** — 근거 승격 금지(나열된 사실의 인과·수단 결합, 근거 없는
  정도 부사 금지, §7) · 자연성 anti-pattern 확장(문어체 서술어·명사 적층·연어·어미 부조화) ·
  전역 표현 반복 점검(특이 서술어 그룹 간 반복 — copy-rules·pipeline §8-4).
- **운영 정책 확정(2026-07-15)** — 전체 카피 생성 목표 개수 **필수 질문**(무응답 진행 금지),
  목표>품질 상한이면 **워닝 후 목표대로 강행**(초과분 로테이션 변형 표시 — §7·pipeline §3·§8-1).
  구 정책(상한까지만 기본·적게 출력 허용)은 폐지.
- **자연성 일반화(2026-07-15, 5차 후속)** — 광고주 불문 **자연성 3원칙** 신설(입말 테스트·
  처음 보는 조합 의심·압축은 내용으로 — 어휘 열거식 anti-pattern은 예시로 격하, §6·copy-rules)
  · 대량 생성 §8-2에 **자연성 전용 재독**(규칙과 분리된 입말 재독) 신설(pipeline).
- **소요시간 규율(2026-07-15, 3차 실측 100광고 폭증 대응)** — 품질 루프 상한 고정: 청크
  시퀀스 validate(≤2회)→자연성 재독(1회)→반환, 문장별 검증 루프 금지, §6-3 의미 검수는
  한 번 훑기·항목 동시 적용, 청크당 광고 12~18개 분할. 상한 초과 문제는 고치지 말고
  플래그로 검수 이관(pipeline·copy-rules).
- **카피 매력도 개선(2026-07-16, 6차 — `workbooks/에이전트 생성 로직 수정 요청사항_0716.pdf`)** —
  설명형·정보형 문구 지적: title은 기능 설명·리포트형이 아니라 **고민·질문·상황을 먼저** 건드리고,
  copy는 title 맥락을 이어 **변화·혜택·판단 포인트**를 드러낸다(**너무 안전해서 매력 약한 문장도
  재작성 대상** — 단 매력은 표현·구조로, 사실성이 항상 우선). 공통 생성 구조 **고민 자극→해결
  방향→혜택/판단 포인트**, 그룹 내 **표현 유형 6종**(고민해결·혜택체감·비교판단·이용방법·결과기대·
  행동유도) 혼합·상투 종결 변주(§6·copy-rules §광고 매력도, pipeline §6-3·§8-2 재독 통합 —
  신규 기계 검사·플래그 없음, 시간 규율 유지).
- **0716 반영 산출물 검토 후속(2026-07-20, 실측 90광고)** — 실패 패턴 5종을 광고주 불문 일반
  원칙으로 승격(특정 문구 금지 아님 — 실측 문구는 예시): ①**정보 공백을 안내문으로 메우지 마라**
  ("공식 안내에 있어요"류·순환 답변·방향 없는 비교 — 각도를 다른 사실로 전환하거나 미생성,
  copy-rules §광고 매력도) · ②**같은 퍼널 단계 그룹 간 판단 축 배타 배정**(전환 장치가 하나면
  신청전환 카피가 "써보고 결정" 한 각도로 수렴 — 실측 7건, context-expansion) · ③**의미 중복
  판정에 해결 각도 추가·title 훅 패턴 전역 점검**(pipeline §8-4) · ④**상한≠금지**(CTA 30% 상한이
  행동유도형 전역 0건으로 과잉 교정 — 6종 중 전역 0건 유형 점검) · ⑤**화자 시점 고정**(구매자≠
  사용자 상품에서 "내 발음" 류 시점 튐 금지, copy-rules 자연성 anti-pattern).
- **플래그 어휘 보강(2026-07-20, 재생성 산출물 검수)** — `validation_status`에 8종 외 임의 라벨
  금지("법무 검수 항목" 실측 — R6 트리거 미포착으로 플래그 집계에서 조용히 누락). 법무·심의
  사안도 `정책 확인 필요:` 접두로 기록(§7·pipeline §5).
- **title-copy 연결성·매력도 보완(2026-07-22, 7차 — `workbooks/에이전트 생성 로직 수정 요청사항_0722.pdf`)** —
  광고주 불문 일반화(실측 문구는 예시): ①**고민 축 일치** — copy는 title이 제기한 고민과 **같은
  축**에서 답한다(축 매핑 표는 예시 — 축 어긋남=범용 전환 장치 연결 금지) · ②구체 고민 title에
  copy **기능 전체 나열 확산 금지** · ③**정의/안내문형 copy 종결 금지**("~하는 방식이에요"류) ·
  ④**추상·과압축 title 금지**(바로 이해되는 고민형·질문형·상황형으로) · ⑤copy **우선 반영 요소
  5종**(해결 방향·판단 포인트·이용자 효익·행동 이유·변화 기대) 중 1개 이상 — 5종은 개별 copy
  최소 성분, 0716 표현 유형 6종(그룹 분포)과 별개(§6·copy-rules S5·§광고 매력도, pipeline
  §6-3·§8-2 재독 통합 — 신규 기계 검사·플래그 없음, 시간 규율 유지).

---

## 4. 도메인 모델

### 역할 & Human-in-the-loop (절대 생략 금지)
`광고주/대행사` → (정보 제공) → `AI 에이전트` → (생성) → `운영자` → (검수·수정) → `광고주` → (확인) → 업로드 → (시스템에서 max_bid 수동 설정)
- **AI는 `adgroups`·`ads`를 생성만 한다. 자동 업로드하지 않는다.** 운영자 검수와 광고주 확인을
  거친 뒤에만 최종 파일이 만들어진다.
- **max_bid는 최종 파일에서 항상 빈칸**(2026-07-02 확정: 값을 넣으면 업로드 오류 발생).
  업로드 후 광고 시스템에서 운영자가 수동 입력한다.

### 통합 워크북 시트 구조
| 구분 | Sheet | 작성 주체 | 역할 | 공식 업로드 |
|---|---|---|---|---|
| 입력 | `campaigns` | 광고주/대행사 | 캠페인명·예산·기간·목표·국가 | **포함** |
| 입력 | 상품·브리프 | 광고주 | 상품·혜택·타깃·니즈·조건·대표 랜딩 URL | 제외 |
| 입력 | 이미지소재 | 광고주 | `image_link`·이미지 메시지·규격·소재별 랜딩 URL | 제외 |
| 입력 | 고객질문·검색데이터 | 광고주 | FAQ·상담·검색 자료 링크 | 제외 |
| 입력 | 상품별 주력 키워드 | 광고주 | SA·사이트 검색어·세일즈 키워드 | 제외 |
| 입력 | 정책·참고자료 | 광고주 | 톤앤매너·금지어·법무/심의 기준 | 제외 |
| 출력 | `adgroups` / `ads` | **AI 생성 + 운영 검토** | Context Hints·제목·카피 자동 생성 | **포함** |

### 생성 결과 → OpenAI 공식 템플릿 필드 매핑
| 생성 결과 | 템플릿 필드 | 처리 |
|---|---|---|
| 캠페인명 | `campaigns.campaign_name` | 광고주·대행사 입력 |
| 광고그룹명 | `adgroups.adgroup_name` | AI 생성 |
| Context Hints | `adgroups.keywords` | AI 생성 → **JSON 배열** |
| 광고명 | `ads.ad_name` | AI 생성 — **실물 시트에 컬럼 없음** → 내부 관리 필드(추적·중복 방지용), 업로드 파일에서 제외 |
| 제목 | `ads.title` | AI 생성 |
| 카피 | `ads.copy` | AI 생성 |
| 랜딩/이미지 URL | `ads.link`, `ads.image_link` | 입력값 자동 연결 (하나라도 없으면 알람) |
| max_bid | `adgroups.max_bid` | **항상 빈칸** — 업로드 후 시스템에서 수동 입력(값 넣으면 오류) |

- **최종 업로드 파일은 공식 3개 시트(`campaigns` / `adgroups` / `ads`)만** 추출한다.
- **공식 시트 확정 컬럼**(실물 캐츠잉글리시 시트 기준, §3):
  - `campaigns`: campaign_name · budget_max · budget_type · launch_date · end_date · objective · target_countries(JSON 배열 문자열, 예 `["KR"]`)
  - `adgroups`: campaign_name · adgroup_name · max_bid(빈칸) · keywords(JSON 배열 문자열)
  - `ads`: adgroup_name · title · copy · link · image_link
- **근거 추적 필드**(검수·설명용, 별도 관리): `source_type`, `source_url`, `source_excerpt`,
  `generation_basis`, `validation_status`, `review_comment`, `confidence_score`.
- **정책·검증 제어 필드**(generated.json 전용, 업로드 파일 제외): 최상위 `policy.banned_terms`
  (공통+SKU별 금지어 병합 목록). validate가 `banned_term`으로 기계 검사.
- **카피 핵심 반영 요소**(워크북 v2, 상품·브리프 시트 — 구 "필수 포함 문구" 대체): SKU별 혜택·조건·
  특징 키워드/짧은 구문 **후보군**(`/` 구분). 광고별로 상황에 맞는 **1~2개 선택·재구성**(동등 의미
  허용, verbatim 의무 없음). 기계 검사 아님 — 생성 가이드·의미 검수. 사용 요소는 `generation_basis`에 기록.

---

## 5. 생성 파이프라인 (순서 고정)

```
입력 분석·구조화
  → 대화 맥락 확장  (SKU × Persona × 고객 문제 × 상황 × 구매 여정·의도 × 메시지 방향)
  → 광고그룹 생성   (유사한 대화 주제·메시지 방향 묶음 = 운영 단위)
  → Context Hints  (광고그룹당 5~10개 이상, JSON 배열)
  → 제목·카피 세트 대량 생성  (Context Hints를 충분히 커버, 다양한 메시지 각도)
  → 이미지·랜딩 URL 연결
  → 자동 검수·재생성
  → 근거 추적 기록
  → 공식 시트 출력
```

- **상품 정보를 바로 카피로 바꾸지 마라.** 대화 맥락을 먼저 구조화한다.
- **모든 조합을 기계적으로 만들지 마라.** 상품이 **자연스러운 해결책이 되는 맥락만** 선별한다.
- **페르소나는 (광범위 상품에서) 독립 확장 축이다.** 브리프 핵심 타깃은 seed일 뿐 — 그 상품을
  자연스럽게 쓰는 사용자군을 추론해 확장한다(카드·통신 등에서 폭발; 단일 서비스는 문제 축에
  붙어 collinear). SKU×퍼널 격자 위에 페르소나를 얹는다.
- **breadth 우선(문맥광고).** 필터는 명백히 부자연하거나 정책 제외인 칸만 컷하고 그럴듯한
  맥락은 남긴다. 품질은 사실 근거·저중복·정책 준수이지 맥락 수 축소가 아니다.
- **매칭 제외 주제(하드 게이트·추론 금지):** 채무·연체·신용등급·미성년자 발급·도박/사행성·
  개인 금융 취약성 추정에 걸리는 페르소나·맥락은 생성 제외.
- **출처 우선순위(충돌 시 상위 우선):** ①법무·심의·정책 → ②광고주 브리프 → ③대표 랜딩 URL →
  ④소재별 랜딩 URL·이미지 내 문구 → ⑤고객 질문·검색 데이터 → ⑥AI 추론.

### 구매 여정 퍼널 (v2 신설 — 생성·분류의 핵심 축)

`①문제정의 → ②제품발견 → ③비교검토 → ④단일제품평가 → ⑤신청전환 → ⑥사용도움`
— **6개 고정 토큰**(2026-07-13 수정 요청 확정). 임의 명칭(문제발견·탐색발견·구매신청 등) 금지.

- 퍼널 단계는 광고그룹·Context Hints·제목·카피 생성의 **핵심 분류 축**. **동일 상품도 단계별로
  질문 맥락과 메시지 초점을 달리해** 생성한다(예: 비교검토 단계 → "생활비 혜택이 좋은
  체크카드는 뭐가 있을까?" 힌트, 비교 기준·혜택 차이 중심 카피). 이 **SKU×단계 전개가 중복 없는 대량 확장의 1차 레버**다 —
  상품당 한 단계씩만 켜는 대각선 생성을 피하고, 각 단계 카피는 그 단계의 질문에 답한다
  (힌트 개수 자체는 카피 수를 늘리지 않는다 — 힌트는 매칭 표면).
- Context Hints는 각 단계에서 사용자가 **실제 입력할 법한 질문·고민·상황**으로, 제목·카피는
  해당 단계의 **정보 요구 수준**에 맞춰 작성한다.
- 전 단계를 기계적으로 동일 수량 생성하지 말고 **캠페인 목적·상품 특성에 따라 비중을 조정**한다.
  사용도움 단계는 선택 적용.
- 퍼널 단계는 **구매 가능성 단정이 아니라 질문 목적·대화 맥락의 분류 기준**이다(§7 민감 주제
  규칙과 함께 적용). 비교·평가 단계에서 경쟁사 비방·확인되지 않은 우월성/비교 표현 금지.
- **운영자 설정:** 포함·제외할 퍼널 단계, 퍼널 단계별·세부 의도별 생성 비중, 상품·광고그룹별
  개별 적용. AI 추천 비중을 기본값으로 제공하고 운영자가 조정한다. **전체 카피 생성 목표
  개수만은 필수 질문 — 반드시 답을 받고 시작한다(§7).**

### 광고그룹·Context Hints 생성 기준 (v2 구체화)

- **광고그룹 판단 흐름:** 입력 정보(SKU·Persona·상황) → 구매 여정(단계·세부 의도) → 운영 조건
  (랜딩·정책·입찰) → 그룹 결정(묶기·분리·캠페인 분리).
  - 구매 여정 단계·세부 의도가 유사하면 묶고, 여정 단계·의도가 달라 메시지 방향·Context Hints·
    랜딩 연결을 동일하게 운영하기 어려우면 나눈다. 단, **여정 단계가 다르더라도 메시지 방향·랜딩·
    정책 조건을 동일하게 운영할 수 있으면 같은 그룹으로 묶을 수 있다.** 이때 adgroup_name의
    구매여정 슬롯에는 **대표 단계 1개**(고정 토큰)만 기록한다.
  - 캠페인 목적·국가/지역·예산/기간이 다르면 광고그룹이 아니라 **캠페인 분리** 기준으로 관리.
- **Context Hints 생성 유형:** 구매 여정 유형(문제정의형/제품발견형/비교검토형/단일제품평가형/
  신청전환형/사용도움형) × 표현 형식(주제형/질문형/문제형/상황형)을 혼합해 생성. **질문형·상황형
  문장 우선**(주제형=검색어형은 낮은 비중) — Context Hints는 검색 키워드가 아니라 ChatGPT 대화
  맥락 정의 기준이다(예: `초등 영어 학습지 추천` → `초등 아이 영어를 집에서 어떻게 시작하면 좋을까?`).

---

## 6. 하드 제약 (형식 검증 규칙)

| 대상 | 규칙 |
|---|---|
| `title` (제목) | 권장 16~18자 / **최대 24자**. 상품명만 반복 금지. **기능 설명형·리포트/안내문형 회피 — 사용자 고민·질문·상황을 먼저 건드린다**(0716, copy-rules §광고 매력도 — 의미 검수). **추상·과압축으로 의미가 흐린 제목 금지** — 처음 읽고 바로 이해되는 고민형·질문형·상황형으로(0722). |
| `copy` (카피) | 권장 32~36자 / **최대 48자**. 제목·동일 혜택 반복 금지 — copy는 title 반복이 아니라 맥락을 이어받아 **구체 혜택·방법 보완**(문제 제기→해결→혜택). **title 어절 단순 반복 금지**(기계 검사 `title_copy_overlap`, warning). **title 고민 반복·재질문 금지** — title이 질문이면 copy는 질문으로 끝나지 않고 해결로 답한다(기계 검사 `title_copy_question_repeat`, warning). **자연성 3원칙(copy-rules — 광고주 불문 일반 검사): 입말 테스트 · 처음 보는 조합 의심 · 압축은 내용으로(조사·필수 성분 깎기 금지)** — 설명문투·명사 적층·연어 오류·어미 부조화 등 anti-pattern 목록은 예시(copy는 title보다 엄격). 특이 서술어의 그룹 간 반복 금지(병합 후 전역 점검). 제공된 사실 내에서만 — 근거 승격 금지(§7). **기능만 설명하거나 너무 안전해서 매력 약한 문장 회피** — 구조는 고민 자극→해결→혜택/판단 포인트(0716, 매력은 표현·구조로 — 사실성 우선). **정보 공백을 안내문("공식 안내에 있어요"류)으로 메우지 마라** — 다른 사실로 각도 전환 또는 미생성(0720). 화자 시점은 타깃 페르소나로 고정(0720). **title 고민 축과 같은 축에서 답한다**(0722, copy-rules S5 고민 축 일치) — 축 어긋남(범용 전환 장치 연결)·기능 전체 나열 확산·정의/안내문형 종결("~하는 방식이에요"류) 금지, **우선 반영 요소 5종**(해결 방향·판단 포인트·이용자 효익·행동 이유·변화 기대) 중 1개 이상 — 의미 검수. |
| 카피 다양성 | 동일 광고그룹 내 문장 형식(질문/공감/정보/혜택/비교형)·종결 형태(질문/제안/정보/비교/행동유도형 등) 분산. **copy는 완결 문장만**(명사구·조건절 종결 금지 — 기계 검사 `copy_sentence_form`, warning; title은 구문형·명사형 허용). **해요체·합니다체 통일**(반말 평서형 금지 — 기계 검사 `copy_speech_level`, warning). `~하세요/~합니다/~있습니다` 과다 반복 제한. **표현 유형 6종 혼합(0716)** — 고민해결/혜택체감/비교판단/이용방법/결과기대/행동유도형이 그룹 내에 섞이게, 상투 종결(`확인할 수 있어요`·`체험할 수 있어요`·`제공합니다` 류) 반복 시 변주(copy-rules 개선표). **CTA형 종결은 그룹 내 30% 이하**(기계 검사 `copy_cta_ratio_30`, warning) — 단 **상한≠금지**: 6종 중 전역 0건 유형(특히 행동유도형)이 있으면 과잉 교정 점검(0720). |
| 금지어 | 정책 시트의 **공통 금지 + SKU별 금지 표현**. `title`·`copy`·`keywords`에 포함 시 오류. 기계 검사(`policy.banned_terms`). |
| 카피 핵심 반영 요소 | SKU별 **후보군** — 반드시 포함 아님. 광고별 상황에 맞게 **1~2개 선택·재구성**(동등 의미 허용). 기계 검사 없음(생성 가이드·의미 검수). |
| `keywords` (Context Hints) | 광고그룹당 **5~10개 이상**, JSON 배열, 한국어/영어. **수량 채우려 관련성 낮은 힌트 생성 금지.** **검색어형 40%±10(30~50%) / 질문·상황형 60%**(범위 밖이면 기계 검사 `keyword_searchform_ratio` warning). 검색어형은 그룹의 상품/SKU·타깃·세부 의도·구매여정 중 **2개 이상이 드러나게 구체화**(범용 검색어형 금지). 해당 그룹의 SKU·세부 의도·구매여정 문맥만 포함(타 그룹 상품·혜택·여정 혼입 금지). 그룹 간 동일 힌트 중복 금지 — **캠페인 무관 전역, 공백만 다른 변형 포함**(기계 검사 `keyword_cross_adgroup_duplicate`, warning) — 범용 문구는 그룹 목적에 맞게 구체화. 한 힌트에 서로 다른 여정 의도(무료체험·비교·신청 등) 혼합 금지. |
| 이미지 | 정사각형 PNG/JPG, **1:1**, **640×640 ~ 1200×1200px**, 로고 메인 금지(로고 메인 여부는 AI 판별 후 운영자 확인). |
| `adgroup_name` | 형식 `상품/SKU_타깃_세부의도_구매여정`(`_` 구분 4슬롯 이상). **마지막 슬롯은 §5 6개 고정 토큰 중 하나**(중복 순번 접미 `_2` 등이면 그 직전 슬롯으로 판정 — 기계 검사 `adgroup_name_format`, warning). 3~1,000자, 공백만 구성 금지, **특수문자 가능**(플랫폼 허용 문자 기준 자동 정규화, 형식 검수 대상), 중복 시 순번/고유 코드. 예: `체크카드_직장인_생활비혜택비교_비교검토`. |
| `ad_name` | 형식 `campaign 또는 SKU 코드 + adgroup 코드 + creative 순번`, 중복 금지. **순번(끝 숫자) 앞 프리픽스는 광고그룹마다 고유**(기계 검사 `ad_name_format`, warning). |
| URL | 접근 가능 여부·HTTPS·리디렉션 도메인·프로모션 종료·이미지 만료 검증. **접근 실패 시 생성 전에 오류 표시.** |
| 수치·가격·기간 | 랜딩페이지와 **정확히 일치**해야 한다. |

**업로드 전 검증(필수):** `campaign`/`adgroup` 참조 무결성 · JSON 유효성 · 중복 `ad_name` ·
URL 형식 · 필수값 · 숫자 타입.

---

## 7. 반드시 지켜야 할 규칙 (YOU MUST / NEVER)

- **사실성:** 제공된 사실 안에서만 생성한다. **미제공 혜택·조건을 지어내지 마라(환각 금지).**
  `ai_inferred`는 **맥락(고민·상황) 추론 전용** — 상품 기능·지표·제3자 사용 등 **사실 주장 금지**,
  근거 없으면 생성하지 않거나 `근거 확인 필요` 플래그. **근거 승격 금지** — 근거가 나열한 사실을
  인과·수단으로 결합("~바탕으로")하거나 정도 부사(객관적으로 등)로 강화하지 마라: 관계·정도에도
  근거가 필요하다(copy-rules 근거 승격 금지·pipeline 의미 검수 대조).
- **근거 추적:** 모든 생성물에 출처(`source_*`)와 `generation_basis`(SKU·Persona·고객 문제·
  상황·**구매 여정 단계·세부 의도**·메시지 방향)를 남긴다. 사실을 인용한 광고는 `source_excerpt`
  발췌 필수(`ai_inferred` 제외 — 기계 검사 `source_excerpt_missing`, warning). 근거 없으면
  `validation_status = 근거 확인 필요`.
- **정보 충돌:** 브리프·랜딩·이미지·소재별 URL 간 정보가 다르면 **자동 생성하지 말고 '광고주 확인 필요' 표시.**
- **자동 검수의 지위:** 자동 검수는 **오류 후보 선별일 뿐**이다. 최종 판단·승인은 **운영자·광고주**가 한다.
- **검수상태 열(0715 확정):** `validation_status`=에이전트 검수 결과, `검수상태`=광고주 검수 결과
  입력란. **검수상태는 전 행 빈칸 디폴트 — AI·CLI 누구도 미리 기입하지 않는다**(자동 승인 기입도
  어디에도 없다). `광고주 확인 필요`는 validation_status 전용 플래그 — 검수상태에 들어오면
  review-in이 Problems로 보고한다.
- **플래그 행 트리거(R6):** `validation_status`에 `의미 중복 후보`/`경고`/`광고주 확인 필요`/
  `길이 초과`/`길이 권장 초과`/`근거 확인 필요`/`정책 확인 필요`/`문장 자연성 확인 필요` 중
  하나라도 포함된 행(기계 검사 `오류(...)` 병합 행 포함)은 플래그 행 — review-out 요약에
  건수로 집계되고, `무수정 승인`으로 덮이면 review-in이 `flagged_approved`로 표면화 —
  finalize는 건수·목록 보고 후 운영자 확인을 받고 진행한다(차단 아님). **8종 외 임의 라벨
  금지**(예: `법무 검수 항목` — 0720 실측): 트리거에 안 걸려 집계 누락, 법무·심의 사안도
  `정책 확인 필요` 접두로 기록한다.
- **경쟁사명:** 기본 제외. 광고주가 입력해도 자동 사용 금지, **승인된 이름만** 허용 목록에서 호출.
  사용 승인·권리/비교광고 검토 여부는 **별도 필드로 관리**. 비방·우월성 단정·확인되지 않은 비교
  표현은 **생성 금지**. 경쟁사명만 포함되거나 자사 상품과 해결 연관성 없는 Context Hints는 제거.
- **개인정보:** 상담·챗봇 데이터의 이름/전화번호/이메일/카드번호 등은 **비식별화**한다.
- **민감 주제:** 금융·건강 등 개인 상태를 추정하는 표현 금지.
- **프로모션 종료:** 종료일이 지난 소재는 생성·출력 대상에서 자동 제외.
- **질문 출처 구분:** 실제 고객 데이터에서 추출한 질문과 AI가 추론한 예상 질문을 **결과에서 구분 표시**.
- **목표 개수 필수 수집(2026-07-15 확정):** 전체 카피 생성 목표 개수는 생성 시작 전 **운영자에게
  반드시 질문해 받는다** — 무응답·기본값 진행 금지(운영자가 못 정하면 AI 추천값 제시 후 확정).
  목표가 품질 유지 상한을 넘으면 **워닝만 제시하고 목표대로 강행** — 초과분은 명시적 로테이션
  변형(`변형=로테이션` 기록, 의미 중복 후보 우선 검토)으로 만든다. **무표시 동일 의미 반복
  생성은 금지**(품질 문제는 줄여서 숨기지 않고 표면화해 검수로 넘긴다).
- **검수 상태값(검수상태 열, 광고주 판정 4종):** `무수정 승인` / `수정 후 승인` / `사용 불가` /
  `부분 재생성`. 빈칸=미검수(최종 파일 제외·보고). `광고주 확인 필요`는 여기에 없다 —
  validation_status 전용 플래그다(0715).

---

## 8. 범위: 1차 vs 2차 (경계를 넘지 마라)

- **1차 (지금 만든다):** 워크북 읽기 → 맥락 확장(Persona·문제·상황·**구매 여정 단계·세부 의도**·
  메시지 방향 추출) → 광고그룹·Context Hints·제목·카피 생성 → 글자수·금지어·사실성·중복·형식·URL
  검수 → 운영자 조건 설정(수량·우선순위·퍼널 비중·검수 기준) 및 결과 검토 화면 → 승인/재생성 →
  근거·제외 사유 표시 → 공식 `campaigns`/`adgroups`/`ads` 파일 출력.
- **2차 (후속, 지금 구현하지 마라):** 성과 API 자동 연동(Advertiser API Insights; 전환·CPA·ROAS
  분석은 별도 전환 측정 설정 필요), CTR·CVR·CPA·ROAS 기반 최적화, 우수·저성과 분석 기반 카피
  재생성, 실시간 정책 자동 업데이트, 법무 검수 자동화, 계정별 대시보드, 광고주별 정책 템플릿화,
  우수 카피 자동 추천·교체.

---

## 9. 코드 스타일 & 컨벤션

- **스펙 필드명을 그대로 사용한다:** `campaign_name`, `adgroup_name`, `keywords`, `ad_name`,
  `title`, `copy`, `link`, `image_link`, `max_bid`, `source_type` 등. **임의로 개명하지 마라.**
- Context Hints는 항상 `keywords` 필드에 **JSON 배열**로 넣는다.
- 사용자·문서 커뮤니케이션은 **한국어**, 코드 식별자·주석 키워드는 영어.
- 광고그룹명·광고명에 **특수문자 사용 가능(v2 확정)**. 단, 플랫폼 허용 문자 기준으로 자동
  정규화하고 형식 검수 대상에 포함한다.
- 임시/실험 파일은 저장소가 아니라 세션 스크래치패드에 둔다.

---

## 10. 용어집

- **SKU** — 광고할 상품·서비스 단위. **Persona** — 상품 니즈가 있는 고객 유형.
- **구매 여정(퍼널)** — `문제정의 → 제품발견 → 비교검토 → 단일제품평가 → 신청전환 → 사용도움`
  6단계 **고정 토큰**(임의 명칭 금지). 구매 가능성 단정이 아니라 **질문 목적·대화 맥락의 분류 기준**(§5).
- **Context Hints** — 광고가 관련될 수 있는 대화 주제·질문·문제·상황·의도를 설명하는 신호.
  정확 일치 키워드가 아니라 **문맥 매칭용 입력값**이며, 업로드 시 `keywords` 필드에 매핑된다.
- **대표 랜딩 URL** — 상품 이해·기본 정보 분석의 기준. **소재별 랜딩 URL** — `ads.link`에 우선
  적용(미입력 시 대표 랜딩 URL).
- **운영자** — 생성 조건 설정·검수·다운로드 담당. **광고주/대행사** — 정보 제공·최종 확인·`max_bid` 확정.
