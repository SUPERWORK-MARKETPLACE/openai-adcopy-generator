# Excel lock files
~$*

# real advertiser upload workbooks — kept local only, never published.
# tests use a de-identified fixture (tool/internal/export/testdata/).
data/

# local-only ops runbook (internal — not for the public repo)
docs/runbook-e2e-and-push.md

# internal design/planning docs — kept local only, not for the public repo
docs/superpowers/

etc/

# OS cruft
.DS_Store
Thumbs.db

# session scratch (progress ledger etc.)
.superpowers/

# adcopy per-run work folders (created next to input workbooks)
*.adcopy/

# oh my opencode
.omc/

# workbooks
workbooks/